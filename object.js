/*var mas = [], i=0, sum=0;
while(true) {
    mas[i] = prompt("enter valuse",0);
    if(mas[i]===""||isNaN(mas[i])||mas[i]===null) {
        break;
    }
    
    sum += +mas[i];
    i++;
    
}
alert(sum);
*/
/*var obj= {
    className: 'open menu'
}
function addclass(obj, cls) {
    var name = obj.className.split(" ");
    for(var i = 0; i<name.length;i++) {
        if(cls===name[i]) {
            return false;
        }
    }
    name.push(cls);
    obj.className = name.join(' ');
}
addclass(obj,'new');
alert(obj.className);
*/
//function logArrayElements(element, index) {
  //console.log('a[' + index + '] = ' + element);
//}
//var mas =[2,3,4];
//mas.forEach(logArrayElements);
/*var mas2 = mas.map(function(obj,index){
   return '['+index+']='+ obj;
})
console.log(mas2);*/
/*var result = mas.reduce(function(obj1,obj2) {
   return obj1+obj2;
})
console.log(result);*/
/*function sum(){
    var summ=0;
    for(var i=0;i<arguments.length;i++) {
        summ+=arguments[i];
    }
    return summ;
}
console.log(sum(1,2,3));*/
